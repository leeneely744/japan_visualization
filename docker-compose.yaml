services:
  dev:
    build: .
    ports:
      - "5173:5173"
    volumes:
      - .:/app
      - /app/node_modules
    working_dir: /app
    command: sh -c "pnpm install && pnpm run dev -- --host"
    stdin_open: true
    tty: true